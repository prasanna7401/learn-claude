---
description: Run full project setup (install deps, generate Prisma client, run migrations)
allowed-tools: [Bash]
---

Run `npm run setup` in the project root to install dependencies, generate the Prisma client, and run database migrations.

After the command completes, report what happened (installed packages, migration status) and remind the user they can start the dev server with `/dev` or `npm run dev`.
